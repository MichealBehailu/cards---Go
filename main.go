package main

import "fmt"

func main() {
	// var card string = "Ace of Spades" //one way to define 
	 card := "Ace of Spades" //this is the same as the above code //the compiler will figure it out (the type)
	 card = "Five of Diamonds" //do assignment
	 fmt.Println(card)
}
